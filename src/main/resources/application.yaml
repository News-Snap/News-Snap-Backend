spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3306/newssnap
    username: 'host'
    password: '0000'
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true